# Verificación

> El agente no dice «funciona» — lo demuestra.

## Obligatorio

- [Comando o test que prueba cada feature]
- `./init.sh` verde antes de marcar `done`

## Anti-patrones

- Marcar `done` sin evidencia ejecutable
- Marcar `done` con `./init.sh` en rojo
